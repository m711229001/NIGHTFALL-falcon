# NIGHTFALL — Autonomous AI VAPT Platform
# Core dependencies
httpx[http2]>=0.27.0
aiohttp>=3.9.0
aiosqlite>=0.20.0
pydantic>=2.7.0
pyyaml>=6.0.1
rich>=13.7.0
typer>=0.12.0

# Web framework (OAST callback server)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# DNS server (OAST)
dnslib>=0.9.24

# Browser automation (crawler)
playwright>=1.44.0

# JWT handling
pyjwt[crypto]>=2.8.0
cryptography>=42.0.0

# Reporting
jinja2>=3.1.4

# Fuzzy matching / diff
python-Levenshtein>=0.25.0

# CLI
click>=8.1.7
shellingham>=1.5.4

# Async utilities
anyio>=4.4.0

# Logging
structlog>=24.1.0

# TLS impersonation (JA3 bypass)
curl_cffi>=0.7.0